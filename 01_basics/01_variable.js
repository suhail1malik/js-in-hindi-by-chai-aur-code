const accountId = 12345;
let employName = "Suhail Mlaik" ;
var city = "shamli" ;
state = "uttar Pradesh" ;
let country ;

// accountId = 4353 ;  This is not allowed in js we can not modify const value 
//  dont use "var" now its not prefer because it has block scope issue and functional scope issue
// if we did not assingn any value in variable then we get undifiened value by default 

console.table([ accountId, employName, city, state, country ]) ;
